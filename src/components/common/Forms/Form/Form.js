import styled from 'styled-components';

const Form = styled.form`
  display: flex;
  flex-direction: column;
  padding-top: 3rem;

  Button {
    align-self: stretch;
    font-size: 2rem;
  }
`;

export default Form;
